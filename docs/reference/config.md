# `config`

Configuración local de la aplicación: dónde vive `OUTPUT_DIR` (documentos procesados) y el
archivo de modelo activo (`config/settings.json`, fuera de `OUTPUT_DIR` a propósito — ver
[003 · Selección de modelo](../features/003-ui-polish-model-switch.md)).

::: config
